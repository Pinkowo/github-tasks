import React from "react";

export interface ISearchSectionProps { }

export const SearchSection: React.FunctionComponent<ISearchSectionProps> = () => {
    return (
        <div>

        </div>
    );
}